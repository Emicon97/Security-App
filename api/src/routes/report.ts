import { Router } from 'express';
const { } = require('../controller/reportController');

const router = Router();

router.get('/', (req, res) => {
   res.send()
})

router.post('/', (req, res) => {
   
})

export default router;